export interface API {
  fleetVehicles: string;
  carsModels: string;
  reservation: string;
  users: string;
  carCategory: string;
  branches: string;
}

export interface SystemSettings {
  baseDomain: string;
}
